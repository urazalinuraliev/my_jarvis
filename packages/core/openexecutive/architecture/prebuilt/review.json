{
  "section_id": "review",
  "title": "SME Knowledge Review",
  "markdown": "Knowledge that flows into retrieval passes through a subject-matter-expert (SME) review gate before it can influence the Executive. The queue and its decisions live in `openexecutive.knowledge.review_store.ReviewStore`, which shares the same `./episodic_memory.db` SQLite database as episodic memory.\n\nItems enter the queue in a pending state. An SME then resolves each one:\n\n- **Pending** — awaiting an SME decision. The Executive never sees pending items; they are excluded from every retrieval until resolved.\n- **Rejected** — filtered out of every retrieval. A rejected item can never surface in a specialist's context.\n- **Approved** — eligible for retrieval, and can be promoted in priority so it ranks ahead of unprioritized material.\n\n### Effect on retrieval\n\nThe gate is a filter layered on top of the normal RAG path: a chunk only reaches a specialist if it is approved (or never required review). Rejection is durable and global — it removes the item from all future queries rather than just down-ranking it. Approval with priority promotion lets an SME push vetted, high-value knowledge to the top of the retrieved set. The net effect is that the curated knowledge the Executive reasons over is exactly the set an SME has signed off on, with rejected material held back and pending material invisible.\n\n### Schema upgrades\n\n`ReviewStore.initialize_db` (through `knowledge.review_schema.ensure_review_schema`, which owns the tables' DDL and value enums) checks both tables against the current shape (every column present with the canonical NOT NULL and primary-key role, `item_id` / `annotation_id` as the sole primary key). A legacy-shaped table — e.g. an integer `id` key, which lets `INSERT OR IGNORE` pile up duplicate registrations — is rebuilt in one transaction with foreign keys off: its rows are copied to `<table>_legacy_<timestamp>` and kept, the salvageable ones are normalized in Python and carried over deduplicated — `item_id`, domain and filename are recovered from one another (`builtin:{domain}:{filename}` / `external:{source}`), legacy `id` / `annotation` columns fill empty `annotation_id` / `correction` ones, a human decision beats a pending duplicate, invalid or NULL enum values fall back to their defaults, and annotations whose item is gone stay in the backup only. Any error rolls the whole rebuild back. Read paths used by retrieval initialize lazily, once per process per database file.",
  "mermaid": "flowchart TD\n  In[\"New knowledge item\"]\n  Queue[\"Pending review queue<br/>ReviewStore\"]\n  SME{\"SME decision\"}\n  Rej[\"Rejected<br/>excluded from all retrieval\"]\n  App[\"Approved<br/>priority promotion\"]\n  RAG[\"Retrieval\"]\n  Spec[\"Specialist context\"]\n  In --> Queue\n  Queue --> SME\n  SME -->|reject| Rej\n  SME -->|approve| App\n  App --> RAG\n  RAG --> Spec\n  class In entry\n  class Queue storage\n  class SME compute\n  class RAG compute\n  class Spec hot\n",
  "generated_at": "2026-09-19T00:00:00+00:00"
}
